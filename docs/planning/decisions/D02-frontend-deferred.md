# D2 · 前端推迟到后端与数据模型完善之后

> 本文件是 `../goal.md` 拆出来的一条决策记录（2026-07-30 拆分，内容一字未改）。
> **`goal.md` 仍是唯一入口**：决策一览表和「去哪找」都在那里，
> 代码注释里写的 `goal.md D2` 指的就是本文件。

**2026-07-29 部分作废。** 原文的重启条件是"**需要做志愿者自助登录、
或需要给外部用户看的页面时**" —— [零](../goal.md#零当前优先级2026-07-29-定)的 P3 恰好就是这句话，
**条件已经满足，不是"要不要提前"的问题**。

**仍然成立的那一半**：不上 React / Vue，不做前后端分离。自助页面就是
**Django 模板 + 视图**，需要局部刷新的地方用 HTMX，和[二、技术选型](../goal.md#二技术选型)一致。
**作废的那一半**："等模型完善了再写界面"—— 志愿者不可能用 Django Admin（[D1](D01-django-postgres-admin.md#d1--django--postgresql--admin-起步) 自己写了
"不能给外部用户用"），所以这几个页面是 P3 的**唯一**实现方式，不能等。
详见 [D21](D21-self-service-and-permissions.md#d21--对外账号志愿者自助页面提前权限成为它的前置条件2026-07-29)。


一开始就上 React/Vue 会让工作量翻倍，而且模型还在变的时候写界面等于白写。
**何时重新考虑**：需要做志愿者自助登录、或需要给外部用户看的页面时。
