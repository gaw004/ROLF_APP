# D12 · 登录账号（User）挂在 Contact 上，与任职状态解耦

> 本文件是 `../goal.md` 拆出来的一条决策记录（2026-07-30 拆分，内容一字未改）。
> **`goal.md` 仍是唯一入口**：决策一览表和「去哪找」都在那里，
> 代码注释里写的 `goal.md D12` 指的就是本文件。

`accounts.User` 有一个可空的 OneToOne 指向 `Contact`；**不指向 `Assignment`**。
**为什么**：三个概念必须分清 ——
- `Contact` = 这个人是谁
- `Assignment` = 这个人在什么时候占着哪个编制（`Position`）
- `User` = 这个人能不能登录、能看什么

它们不是一一对应的：不是每个员工都需要登录账号（比如只领工资不用系统的兼职），
不是每个登录账号都是员工（以后志愿者自助登录、外部审计员只读账号），
还有一个纯技术账号（superuser）根本不对应任何真人 —— 所以 `contact` 必须可空。
一个人有多个 Assignment 但只该有一个登录账号，这也是 User 必须挂在 `Contact` 而非
`Assignment` 上的原因。
**影响**：这条决定了 Phase A 里自定义 User model 怎么建，所以要在动手前定下来。

> ⚠️ **2026-07-29 晚更正。** 原文最后一句是"权限一律用 Django Group
> （管理员 / 项目协调员 / 只读）"。**这句话已被
> [D20](D20-ministry-role.md#d20--范围化权限-ministryrole不走-django-group2026-07-29) 推翻** ——
> Django 权限没有对象级作用域，表达不了"食物银行的 admin"。
> D20 当时写着"已在原地改掉"，**但实际漏改了两处**：这里，以及旧 Phase D 小节
> （那一节同日整体删掉了）。现在的口径是：**带"某个 ministry 的"这个定语的权限进
> `MinistryRole`，真·全局的权限才用 Django Group。**
>
> **本条真正要留下的那半句没有变，而且 D20 还引用了它**：
> **不看"是不是员工"来判断权限** —— 在组织里担任什么职务，和在系统里能操作什么，
> 是两个问题（所以 D20 也拒绝复用 `Position.is_leader`）。
